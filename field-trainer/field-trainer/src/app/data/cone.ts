export class Cone {
    constructor(public name?: string, public ip_address?: string, public id?: number) {}

    // toJSON() {
    //     return {
    //         name: this.name,
    //         ip_address: this.ip_address,
    //         id: this.id
    //     };
    // }
}