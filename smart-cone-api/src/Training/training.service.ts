import { Injectable } from "@nestjs/common";

@Injectable()
export class TrainingService {
    async startSession(): Promise<void> {
        // Setup the training session with provided information
    }
}
